import { themes, fontSizes, languages } from '@constants/index';

export default {
  config: {
    theme: themes.LIGHT,
    fontSize: fontSizes.MD,
    language: languages.ES
  },
  tempConfig: {
    modalIsOpen: false
  },
  _persist: {
    rehydrated: true
  }
};
